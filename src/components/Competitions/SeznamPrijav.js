import classes from "./SeznamPrijav.module.css";

export default function SeznamPrijav() {
  return (
    <div className={classes.page}>
      <form className={classes.card}>
        <h2 className={classes.title}>Seznam Prijavljenih</h2>

       
      </form>
    </div>
  );
}
